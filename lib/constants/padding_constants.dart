import 'package:flutter/material.dart';

class PaddingConsts {
  static const padding1 = EdgeInsets.all(1.0);
  static const padding2 = EdgeInsets.all(2.0);
  static const padding5 = EdgeInsets.all(5.0);
  static const padding10 = EdgeInsets.all(10.0);
  static const padding12 = EdgeInsets.all(12.0);
  static const padding20 = EdgeInsets.all(20.0);

  static const vertical10 = EdgeInsets.symmetric(vertical: 10.0);

  static const horizontal5 = EdgeInsets.symmetric(horizontal: 5.0);
  static const horizontal10 = EdgeInsets.symmetric(horizontal: 10.0);
}
